<Window x:Class="HifiPrototype2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HifiPrototype2"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="3">
            <Button x:Name="addEmployeeButton" Padding="3" Margin="3" Click="addEmployeeButton_Click">
                Add employee
            </Button>
            <Button x:Name="addTask" Padding="3" Margin="3" Click="addTask_Click" >
                Add Task
            </Button>
            <StackPanel x:Name="taskHolder">
                
            </StackPanel>
        
        </StackPanel>
        <GridSplitter ResizeDirection="Columns" ShowsPreview="True" Background="LightGray" Width="3"/>
        <ScrollViewer Grid.Column="2" Margin="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <local:ScheduleView x:Name="Schedule"/>
        </ScrollViewer>


    </Grid>
</Window>
